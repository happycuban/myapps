---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mw-dashboard-http
  namespace: mw-services
spec:
  parentRefs:
  - name: traefik
    sectionName: http
    namespace: kube-system
    kind: Gateway
  hostnames:
  - mw-dashboard-k8s.ops.mindworking.local
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mw-dashboard-https
  namespace: mw-services
  labels:
    gateway: traefik
spec:
  parentRefs:
  - name: traefik
    sectionName: https
    namespace: kube-system
    kind: Gateway
  hostnames:
  - mw-dashboard-k8s.ops.mindworking.local
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: mw-dashboard-homer
      namespace: mw-services
      port: 8080  
    filters: 
    - type: ExtensionRef
      extensionRef:
        group: traefik.io
        kind: Middleware
        name: mw-dashboard-headers
